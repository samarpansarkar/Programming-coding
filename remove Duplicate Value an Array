function duplicateValue(arr){
  let newArr=[];
  let seen={};
  
  for (let i = 0; i < arr.length; i++) {
    if (seen[arr[i]] !== true) {
      seen[arr[i]]=true;
      newArr[newArr.length]=arr[i];
    }
  }
  return newArr;
}

let res = duplicateValue([1, 2, 2, 3, 4, 4, 5]);

console.log(res);
