
function isPal(str){
    let left =0;
    let right=str.length-1;

    while(right>left){
        if(str[left]!==str[left]){
            return false;
        }

        left ++;
        right --;
    }
    return true;
}

let res = isPal("rececar");

console.log(res);
